[pytest]
testpaths = tests
pythonpath = .
base_url = https://www.saucedemo.com
addopts = --tracing retain-on-failure --alluredir=allure-results --clean-alluredir
markers =
    smoke: main flows, quick to run
    ui: browser tests (SauceDemo)
    api: API tests (Automation Exercise)
